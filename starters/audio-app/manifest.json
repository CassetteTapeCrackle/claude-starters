{
  "stack": "audio-app",
  "plugin": null,
  "frameworks": ["juce"],
  "standard": "c++17",
  "package_manager": "cpm",
  "juce_version": "9",
  "skills": ["clean-code-cpp", "clean-code-audio"],
  "commands": {
    "configure": "cmake -B build -DCMAKE_BUILD_TYPE=Debug",
    "build": "cmake --build build",
    "test": "ctest --test-dir build",
    "format": "clang-format -i",
    "lint": "clang-tidy"
  }
}
